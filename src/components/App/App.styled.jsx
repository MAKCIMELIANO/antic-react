import styled from '@emotion/styled';

export const Body = styled.div`
  background-color: ${p => p.theme.colors.backgroundPageWhiteColor};
  color: ${p => p.theme.colors.brownLightTextColor};
  font-family: ${p => p.theme.fonts.body};
`;
